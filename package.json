{
  "name": "flame",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "flame dev",
    "build": "NODE_ENV=production flame build",
    "preview": "flame preview",
    "deploy": "flame deploy"
  },
  "dependencies": {
    "@docubook/flame": "^2.0.2"
  }
}
