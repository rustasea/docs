{
  "name": "rustasea-docs",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.1"
  },
  "scripts": {
    "dev": "flame dev",
    "build": "NODE_ENV=production flame build",
    "preview": "flame preview",
    "deploy": "flame deploy",
    "audit": "bun audit"
  },
  "dependencies": {
    "@docubook/flame": "^2.0.3"
  }
}
